# Copy this file to .env and add your own Anthropic API key to enable
# live AI responses (News Translator, Coach, Quick Prep, Discover briefs).
# Get a key at https://console.anthropic.com
#
# Without this, the app still runs and every AI feature gracefully
# falls back to a static demo response.
VITE_ANTHROPIC_API_KEY=
